// Fill these in with your own Firebase project's values (see ../README.md).
// The Web API key is not a secret — access is controlled by Firestore
// security rules, not by hiding this key.
export const FIREBASE_CONFIG = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  projectId: "noslop-a3f21",
};
