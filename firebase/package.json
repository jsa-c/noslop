{
  "name": "noslop-firebase",
  "version": "1.4.0",
  "private": true,
  "description": "Firebase CLI project config for NonSlop (Firestore rules + Cloud Functions).",
  "scripts": {
    "login": "firebase login",
    "use": "firebase use --add",
    "deploy": "firebase deploy --only firestore:rules,functions",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:functions": "firebase deploy --only functions",
    "emulators": "firebase emulators:start --only firestore,functions"
  },
  "devDependencies": {
    "firebase-tools": "^15.0.0"
  }
}
