{
  "name": "noslop-functions",
  "version": "1.1.0",
  "description": "Cloud Functions for NoSlop — keeps per-video slop aggregates in sync with individual votes.",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
