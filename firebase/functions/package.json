{
  "name": "noslop-functions",
  "version": "1.2.0",
  "description": "Cloud Functions for NonSlop — keeps per-video slop aggregates in sync with individual votes.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
